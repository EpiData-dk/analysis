new p !size := 10;
new v v1 int;
new g i  i;

for i := 1 to size(df1) do
begin
  ? "I = " + i;
  ? "V1[" + i + "] = " + V1[i];
end;


for i := 1 to size(df1) do
  ? i * 10;

new g j i;
for i := 1 to size(df1) do
begin
  for j := 1 to 4 do
    ? i * 10 +j;
  ? "V1[" + i + "] = " + V1[i];
  for j := 1 to 4 do
    begin
      ? i * 100 + j * 10;
      ? "V1[" + i + "] = " + V1[i] * 10;
    end;
end;

